export const WINDOW_VARIABLE = {
    BROKER: 'BrokerSharedVariables'
}

export const SHARED_ATTRIBUTE = {
    STATE: 'state',
    BUS: 'bus'
}

export const PUBLISH_CODE = {
    STARTED_FETCH = 'started-fetch',
    FINISHED_FETCH = 'finished-fetch',
    INITIALIZED = 'initialized',
    DESTROYED = 'destroyed'
}